import {
  CheckCircle2,
  Headphones,
  Minus,
  PackageCheck,
  Plus,
  RotateCcw,
  ShieldCheck,
  ShoppingCart,
  Tag,
  Trash2,
  Truck,
  Wrench,
} from "lucide-react";
import { useMemo } from "react";
import { Link } from "react-router-dom";
import ProductCard from "../components/ProductCard";
import { useCommerce } from "../context/CommerceContext";
import { money, recommendationsFor } from "../lib/products";
import { computeTotals } from "../lib/orderTotals";

export default function Cart() {
  const { cart, products, setQuantity, removeFromCart, addToCart, couponApplied, setCouponApplied } = useCommerce();
  const items = cart.map((item) => ({ ...item, product: products.find((product) => product.id === item.productId) })).filter((item) => item.product);
  const { subtotal, installationFee: installation, shipping, discount, insurance, total } = computeTotals(items, { coupon: couponApplied, secureShipping: false });
  const suggestions = useMemo(() => {
    const ids = new Set(items.map((item) => item.productId));
    return [...new Map(items.flatMap((item) => recommendationsFor(item.productId)).filter((product) => !ids.has(product.id)).map((product) => [product.id, product])).values()].slice(0, 4);
  }, [items]);
  const addInstallation = () => items.filter((item) => item.product.installationEligible && !item.installation).forEach((item) => addToCart(item.productId, 0, true));

  if (!items.length) return <main className="min-h-screen bg-slate-50 py-16"><div className="mx-auto max-w-xl rounded-2xl bg-white p-12 text-center shadow-sm"><ShoppingCart className="mx-auto text-slate-300" size={52} /><h1 className="mt-5 text-2xl font-extrabold">Your cart is empty</h1><p className="mt-2 text-slate-500">Find the right IT and security products for your setup.</p><Link to="/products" className="mt-6 inline-block rounded-lg bg-amber-500 px-6 py-3 font-bold text-slate-950">Continue shopping</Link></div></main>;

  return <main className="min-h-screen bg-slate-50 py-7"><div className="mx-auto max-w-[1450px] px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between"><div><p className="text-sm text-slate-500">Home / Shopping cart</p><h1 className="mt-2 text-3xl font-extrabold text-slate-900">Shopping Cart ({items.length})</h1></div><Link to="/products" className="text-sm font-bold text-blue-700">Continue shopping</Link></div><div className="mt-6 grid gap-6 xl:grid-cols-[minmax(0,1fr)_410px]"><section className="space-y-5"><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 px-6 py-4"><h2 className="text-lg font-extrabold">Items in your cart</h2></div><div className="divide-y divide-slate-200">{items.map((item) => <CartItem key={item.productId} item={item} onQuantity={setQuantity} onRemove={removeFromCart} />)}</div></div><section className="rounded-2xl border border-slate-200 bg-white p-6"><div className="grid gap-6 md:grid-cols-3"><div><p className="flex items-center gap-2 font-extrabold"><Wrench className="text-amber-500" /> Professional installation</p><p className="mt-3 text-sm leading-6 text-slate-600">Certified technicians handle setup, cabling, configuration and customer training.</p><button onClick={addInstallation} className="mt-4 rounded-lg bg-[#071426] px-4 py-2.5 text-sm font-bold text-white">Add installation</button></div><div className="border-y border-slate-200 py-5 md:border-x md:border-y-0 md:px-6 md:py-0"><p className="font-extrabold">Why Honey Vision?</p><div className="mt-4 grid grid-cols-2 gap-4 text-center text-xs"><Stat text="3,000+ Happy Customers" /><Stat text="500+ Expert Technicians" /><Stat text="Pan India Service" /><Stat text="Same Day Support" /></div></div><div><p className="font-extrabold">Installation includes</p><ul className="mt-3 space-y-2 text-sm text-slate-600"><li>✓ Site inspection</li><li>✓ Device setup & configuration</li><li>✓ Neat cable management</li><li>✓ Mobile app configuration</li></ul></div></div></section>{suggestions.length > 0 && <section><p className="text-sm font-bold uppercase tracking-wide text-amber-600">Complete your setup</p><h2 className="mt-1 text-2xl font-extrabold">Frequently bought together</h2><div className="mt-5 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{suggestions.map((product) => <ProductCard key={product.id} product={product} />)}</div></section>}</section><aside className="space-y-5 xl:sticky xl:top-24 xl:h-fit"><OrderSummary subtotal={subtotal} discount={discount} shipping={shipping} installation={installation} total={total} couponApplied={couponApplied} setCouponApplied={setCouponApplied} /><TrustGrid /><div className="overflow-hidden rounded-2xl bg-[#071426] p-6 text-white"><div className="flex items-start justify-between"><div><h3 className="flex items-center gap-2 text-lg font-extrabold"><Wrench className="text-amber-300" /> Need installation?</h3><p className="mt-2 max-w-xs text-sm leading-6 text-slate-300">Add certified installation and get a convenient appointment slot at checkout.</p><button onClick={addInstallation} className="mt-5 rounded-lg bg-amber-500 px-4 py-2.5 text-sm font-bold text-slate-950">Add installation to cart</button></div><Headphones className="text-amber-300" size={52} /></div></div><div className="rounded-2xl bg-blue-50 p-6"><h3 className="text-lg font-extrabold text-slate-900">Bulk order?</h3><p className="mt-2 text-sm text-slate-600">Get special pricing for office, institution and project purchases.</p><Link to="/contact" className="mt-4 inline-block rounded-lg border border-blue-600 bg-white px-4 py-2 text-sm font-bold text-blue-700">Contact sales</Link></div></aside></div></div></main>;
}

function CartItem({ item, onQuantity, onRemove }) {
  return <article className="grid gap-4 p-5 sm:grid-cols-[100px_minmax(0,1fr)_auto] sm:items-center"><img src={item.product.image} alt={item.product.name} className="h-24 w-24 rounded-xl bg-slate-50 object-contain" /><div><Link to={`/products/${item.productId}`} className="font-extrabold text-slate-900 hover:text-amber-600">{item.product.name}</Link><p className="mt-1 text-sm text-slate-500">{item.product.features.slice(0, 3).join(" · ")}</p><p className="mt-2 flex items-center gap-1 text-sm font-semibold text-green-700"><CheckCircle2 size={15} /> In stock · {item.product.delivery}</p>{item.installation && <p className="mt-2 flex items-center gap-1 text-sm font-semibold text-amber-700"><Wrench size={15} /> Installation service added</p>}<div className="mt-4 flex flex-wrap items-center gap-4"><div className="flex overflow-hidden rounded-lg border border-slate-200"><button onClick={() => onQuantity(item.productId, item.quantity - 1)} className="p-2.5 hover:bg-slate-50"><Minus size={16} /></button><span className="grid w-10 place-items-center border-x border-slate-200 text-sm font-semibold">{item.quantity}</span><button onClick={() => onQuantity(item.productId, item.quantity + 1)} className="p-2.5 hover:bg-slate-50"><Plus size={16} /></button></div><button onClick={() => onRemove(item.productId)} className="flex items-center gap-1 text-sm text-red-500"><Trash2 size={16} /> Remove</button></div></div><div className="sm:text-right"><p className="text-lg font-extrabold">{money(item.product.price * item.quantity)}</p><p className="mt-1 text-sm text-slate-400 line-through">{money(item.product.mrp * item.quantity)}</p></div></article>;
}

function OrderSummary({ subtotal, discount, shipping, installation, total, couponApplied, setCouponApplied }) { return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-xl font-extrabold">Order summary</h2>{couponApplied ? <div className="mt-5 flex items-center justify-between rounded-lg border border-dashed border-green-500 bg-green-50 p-3"><span className="flex items-center gap-2 text-sm font-bold text-green-700"><Tag size={17} /> HONEY10 <span className="font-normal">Coupon applied</span></span><button onClick={() => setCouponApplied(false)} className="text-sm font-bold text-green-700">Remove</button></div> : <button onClick={() => setCouponApplied(true)} className="mt-5 w-full rounded-lg border border-dashed border-amber-500 bg-amber-50 p-3 text-sm font-bold text-amber-700">Apply HONEY10 and save 10%</button>}<div className="mt-5 space-y-3 border-b border-slate-200 pb-5 text-sm"><Line label="Subtotal" value={money(subtotal)} /><Line label="Discount (HONEY10)" value={`- ${money(discount)}`} green /><Line label="Shipping" value={shipping ? money(shipping) : "FREE"} green={!shipping} /><Line label="Installation charges" value={money(installation)} /></div><div className="flex items-end justify-between py-5"><span className="font-extrabold">Total amount</span><span className="text-2xl font-extrabold">{money(total)}</span></div><p className="text-right text-xs text-green-700">You save {money(discount)} on this order</p><Link to="/checkout" className="mt-5 block rounded-lg bg-amber-500 px-5 py-3.5 text-center font-extrabold text-slate-950 hover:bg-amber-400">Proceed to checkout</Link></section>; }
function Line({ label, value, green = false }) { return <div className="flex justify-between gap-4 text-slate-600"><span>{label}</span><b className={green ? "text-green-600" : "text-slate-800"}>{value}</b></div>; }
function Stat({ text }) { return <div><ShieldCheck className="mx-auto text-amber-500" size={24} /><p className="mt-1 font-bold text-slate-700">{text}</p></div>; }
function TrustGrid() { return <section className="grid grid-cols-2 gap-px overflow-hidden rounded-2xl border border-slate-200 bg-slate-200"><Trust icon={ShieldCheck} text="Secure payments" /><Trust icon={RotateCcw} text="Easy returns" /><Trust icon={Truck} text="Fast delivery" /><Trust icon={PackageCheck} text="100% original" /></section>; }
function Trust({ icon: Icon, text }) { return <div className="flex flex-col items-center bg-white px-3 py-4 text-center text-xs font-bold text-slate-700"><Icon className="mb-2 text-blue-700" size={20} />{text}</div>; }
